# coulomb-matrix

Python tools to compute Coulomb integrals from Wannier functions.

Install locally (editable):

```bash
pip install -e .
```

Modules live in `src/coulomb_matrix` and top-level helper scripts remain for backwards compatibility.
